# Project defaults (applied when sdkconfig is regenerated).
# 1 ms FreeRTOS tick so the TOE yield-poll loops (vTaskDelay(1)) poll at 1 ms
# instead of rounding up to the 10 ms of the default 100 Hz tick.
CONFIG_FREERTOS_HZ=1000
